spring:
  mail:
    host: 'smtp.gmail.com'
    username: 'gmail-address'
    password: 'gmail-app-password'
    properties:
      mail:
        smtp:
          auth: 'true'
          socketFactory:
            port: '465'
            class: 'javax.net.ssl.SSLSocketFactory'
            fallback: 'false'
          ssl:
            enable: 'true'
mail:
  test:
    username:
      to: 'test-email'